<div class="container">
	<div class="row">
		<div class=col-xs-3>
			<h2>User info</h2>
			<%= render 'users/profile', user: @user %>
			<!--同コントローラーのviewで表示ならusersは省略可 -->
			<%= render 'relationships/follow_button', user: @user %>

			<h2>New book</h2>
			<%= render 'books/newform', book: @book %>
			</div>

		<div class=col-xs-9>
			<h2>フォロワー一覧</h2>
		<!--users一覧 -->
			<%= render 'users/users_table', users: @user.followers %>
		<!--books一覧 -->
		</div>
	</div>
</div>