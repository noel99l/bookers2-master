<div class="container">
	<div class="row">
		<div class=col-xs-3>
			<h2>User info</h2>
			<%= render 'users/profile', user: @user %>
			<%= render 'relationships/follow_button', user: @user %>
			<h2>New book</h2>
			<%= render 'books/newform', book: @book %>
			</div>
		<div class=col-xs-9>
			<h2>フォロー一覧</h2>
			<%= render 'users/users_table', users: @user.followings %>
		</div>
	</div>
</div>